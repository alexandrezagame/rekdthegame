import { makeStyles } from '@material-ui/core/styles';
export default makeStyles(() => ({
  downloadContainer: {
    marginTop: '2rem',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    textAlign: 'center',
  },
  downloadLink: {
    textDecoration: 'none',
    color: 'white',
  },
}));
