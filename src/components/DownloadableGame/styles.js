import { makeStyles } from '@material-ui/core/styles';
export default makeStyles(() => ({
  downloadContainer: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    textAlign: 'center',
  },
  downloadLink: {
    textDecoration: 'none',
    color: 'white',
  },
}));
